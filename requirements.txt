# Pinned exactly so the image is reproducible. Bumped 2026-08-20 after `pip-audit` flagged 26
# advisories across the previous pins; `.github/workflows/security-audit.yml` runs the same
# check weekly, so re-pin from its output rather than letting these drift again.
fastapi==0.141.1
# Not a transitive leftover: fastapi declares `starlette>=0.46.0` with no upper bound, so
# installing fastapi alone happily kept a vulnerable 0.47.x in place. Pinned directly for that
# reason -- 0.47.3 carried the FileResponse Range quadratic-time DoS (PYSEC-2026-1942), the
# StaticFiles UNC-path SSRF that only triggers on Windows (PYSEC-2026-2281, and this app has a
# Windows deployment runbook), and the unvalidated Host/path request.url reconstruction
# (PYSEC-2026-161 / -248). All of those sit on paths this app actually mounts.
starlette==1.6.0
uvicorn[standard]==0.52.4
# 0.0.20 had five DoS/parsing advisories on the multipart path, which is the file-upload route.
python-multipart==0.0.32
# Bumped 2.3.1 -> 3.0.1 on 2026-08-20 to close a dev/prod split: the local .venv had drifted to
# 3.0.1 while the image still built 2.3.1, so the test suite was validating a different major
# version of pandas than production ran. Not a CVE fix -- pandas has no advisories at either
# version -- but 3.0 changed copy-on-write and string-dtype behaviour, so "the tests pass" was
# only true of the version nobody was deploying. All 236 tests pass on 3.0.1.
pandas==3.0.1
openpyxl==3.1.5
httpx==0.28.1
# 2.10.1 had a verifier-side algorithm allow-list bypass (PYSEC-2026-176). backend/auth.py pins
# `algorithms=["RS256"]` specifically to stop alg-confusion attacks on the Cloudflare Access
# token, so that advisory landed directly on this app's only cryptographic check.
pyjwt[crypto]==2.13.0
